type Query {
    allBooks: [Book]
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pagesCount: Int
    author: Author
}

"This is an englich description on Author."
type Author {
    id: ID
    firstName: String
    lastName: String
}